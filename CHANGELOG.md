# Changelog

本文件记录群抽奖系统（`astrbot_plugin_group_lottery`）的版本变更。

## v1.1.2

### 修复

- **私聊里 `抽奖 密钥 查看` 报「群号必须是纯数字，收到的是「查看」」**：省略群号时
  插件把「查看」当成了群号。现在：

  - **只读命令可以省略群号** —— `抽奖 状态` / `抽奖 名单` / `抽奖 密钥 查看`
    （以及裸发 `抽奖 密钥` 准备粘贴密钥）会套用你最近发布过抽奖的那个群，
    回复里写明用的是哪个群；
  - **会改动状态的命令仍必须写明群号** —— `开奖` / `取消` / `定时` / `名额` /
    `满员` / `私聊` / `说明` / `密钥 清空`，避免猜错群误操作；
  - 报错信息里会列出「你最近发布过抽奖的群」，方便补上群号；
  - 顺带修掉一条不准确的提示：机器人不在群里时，之前一律说「无法在该群发布抽奖」，
    即使你执行的是「名额」这类命令。

### 说明

- 单元测试 127 项，新增省略群号的只读命令、破坏性命令拒绝套用默认群等用例。

## v1.1.1

### 修复

- **剩余密钥会「失踪」**：设了 3 个名额 3 条密钥，但只有 2 人报名时，多出的 1 条
  密钥开奖后既没人拿到、也无处可取 —— `抽奖 密钥 查看` 会因为「没有进行中的抽奖」
  而拒绝。现在：
  - 开奖后**私聊发布者**，列出未送出的密钥明文与原因（报名人数不足 / 未开启私聊
    发奖等），群公告里只出现条数；
  - 发布者私聊不通时，公告里给出「抽奖 密钥 &lt;群号&gt; 查看」的自助取回办法；
  - `抽奖 密钥 查看` / `抽奖 密钥 清空` 在没有进行中的抽奖时自动回退到本群最近
    一场，开奖后依然可用。
- **私聊投递结果被忽略**：`send_private_text` 之前无条件返回成功，导致找不到平台
  适配器时仍把中奖者标记为「已送达」。现在会检查 `context.send_message` 的返回值。

### 说明

- 顺带纠正文档里的过度承诺：`Context.send_message` 的返回值语义是「是否找到平台
  适配器」而非「是否送达」。aiocqhttp 下「未加好友 / 被风控」是靠协议端抛
  `ActionFailed` 检测的；适配器若自行吞掉错误，插件无法感知。
- 单元测试 121 项，新增开奖后剩余密钥的归属、取回、清理、私聊失败降级等用例。

## v1.1.0

### 新增

- **一行式发布**：选项可以一次写完，不用分几次发命令。

  ```
  抽奖 发布 754797467 支付宝口令红包5元 2 定时 20:00 满员 8 说明 手慢无
  ```

  支持的选项：`名额 <数字>` / `定时 <时间>` / `满员 <人数>` / `私聊 开|关` /
  `说明 <文本>`。群聊与私聊（带群号）写法一致。奖品名里带数字不会被误判
  （`5元红包 2` → 奖品「5元红包」，2 个名额），`定时 12-31 20:00` 这种带空格的
  时间也能整体取到。参数写错时不会建出场次，直接提示原因。

### 变更

- **期号改为按群独立编号**：`第 N 期` 现在是**本群内**的期号，从 1 开始，不再
  因为其它群发过抽奖而显示成「第 47 期」。内部仍使用全局自增主键关联参与者 /
  密钥 / 中奖记录，面板接口也继续按内部 ID 取详情。升级时自动为历史数据回填
  群内期号，老记录不会作废。

### 说明

- 单元测试 115 项，新增一行式发布解析、群内期号独立编号、期号回填迁移、
  中奖记录期号等用例。

## v1.0.1

### 修复

- **开启 `unique_session` 时抽奖变成「每人一场」**：早期版本直接拿
  `event.unified_msg_origin` 当群标识，而 AstrBot 开启会话隔离后群消息的会话段是
  `{用户ID}_{群号}`（见 `pipeline/waking_check/stage.py`）。后果是管理员发布后：
  - 同群其他人发 `抽奖 参与` 提示「当前没有进行中的抽奖」
  - 私聊按群号设置密钥提示「该群当前没有进行中的抽奖」
  - 公告里的群号显示成 `1626810822_762429641`

  现在统一改用 `event.get_group_id()`（真实群号）构造群级会话标识，与是否开启
  `unique_session` 无关。升级后会自动把历史数据里的隔离标识归一化回真实群号，
  已有的抽奖记录不会作废。

### 说明

- 单元测试 98 项，新增 `unique_session` 回归用例（同群共享抽奖、私聊发布后群内参与、
  开奖公告投递到群级会话、历史数据迁移）。

## v1.0.0

首个版本。

### 新增

- **发布抽奖**：`抽奖 发布 <奖品名> [名额]`，可补充奖品说明，同一群同时只允许一场进行中的抽奖。
- **报名参与**：`抽奖 参与` / `抽奖 退出` / `抽奖 名单` / `抽奖 状态`。
- **三种开奖方式**
  - 手动：`抽奖 开奖`
  - 定时：`抽奖 定时 20:00`，支持 `+2h`、`12-31 20:00`、`2026-12-31 20:00` 等写法
  - 满员：`抽奖 满员 <人数>`，报名达标立即开奖
- **两种奖品形态**
  - 普通奖品：群内公布名单并提示联系群主
  - 密钥奖品：管理员**私聊**粘贴密钥池，开奖后机器人逐个私聊发放专属密钥
- **密钥不上群**：密钥设置强制走私聊，群聊里发送密钥会被拒绝入库并尝试撤回该消息；群里只保留不含密钥内容的「查看 / 清空」。
- **私聊按群号跨群管理**：`抽奖 发布 <群号> <奖品名> [名额]`、`抽奖 密钥 <群号>`、`抽奖 开奖 <群号>` 等，管理员全程不用进群；发布前校验机器人在该群中，并在群里自动播报一条抽奖信息。
- **开奖公告**：真实 @ 中奖者（OneBot 系），其它平台自动降级为文本昵称；支持自定义模板与占位符。
- **自助补领**：私聊投递失败时中奖者发送 `抽奖 领取` 补领，公告里点名提示。
- **管理面板**：总览 / 抽奖场次 / 中奖记录三个标签页，可在线查看密钥池、粘贴保存密钥、直接开奖或取消。
- **持久化调度**：开奖时间存 SQLite，插件重启后自动恢复；错过的开奖会补开，开奖失败会取消定时并提示管理员。
- **群黑白名单**、额外管理员列表、每群历史记录自动裁剪。

### 说明

- 无第三方依赖，仅使用 Python 标准库。
